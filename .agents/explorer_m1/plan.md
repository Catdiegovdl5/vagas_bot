# Plan: Code Review and Platform Evaluation

## Objective
Analyze how the scrapers are integrated into `bot.py`, how `is_job_relevant` works, and evaluate Gupy, Catho, InfoJobs, and Vagas.com.br.

## Tasks
1. Search `bot.py` for references to existing scrapers, search terms, and how `is_job_relevant` is called.
2. Read the source code of `scrapers/gupy.py`, `scrapers/catho.py`, `scrapers/infojobs.py`, and `scrapers/vagas_com.py`.
3. Check what other scrapers exist and how they are structured.
4. Prepare recommendations on platform viability and integration details.
